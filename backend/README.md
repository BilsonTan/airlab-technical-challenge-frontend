# Start backend server by 

### `yarn start`
Runs the app in the development mode.
Open [http://localhost:8080](http://localhost:8080) to test the api.

### `yarn test`
Runs jest to test all spec files

### `yarn test-coverage`
Runs jest with coverage values